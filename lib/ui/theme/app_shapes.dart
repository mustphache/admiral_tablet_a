import 'package:flutter/material.dart';

class AppShapes {
  static const radius = 16.0;
  static RoundedRectangleBorder cardShape   = RoundedRectangleBorder(borderRadius: BorderRadius.circular(radius));
  static RoundedRectangleBorder buttonShape = RoundedRectangleBorder(borderRadius: BorderRadius.circular(12));
}
